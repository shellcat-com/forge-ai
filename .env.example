# Placeholder-only configuration. Copy to .env.local and enter secrets privately.
# Provider adapters are a subsequent milestone; no key is needed for the foundation.
GEMINI_API_KEY=
GROQ_API_KEY=
OPENROUTER_API_KEY=
OLLAMA_BASE_URL=http://127.0.0.1:11434
# Never prefix secrets with NEXT_PUBLIC_ or VITE_.
