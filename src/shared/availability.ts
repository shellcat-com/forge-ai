/** User-facing availability shared by the API and the workspace. */
export const hostedGenerationUnavailable =
  'Website generation is not connected on this installation. Hosted model connections and the approved cloud build runtime still need integration.'
