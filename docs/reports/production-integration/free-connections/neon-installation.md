# Live Neon foundation — connected release still incomplete

2026-09-10. Organization `org-solitary-bonus-96047632` reports plan `free`. Existing project `flat-pond-32100882` (`forge-auth-staging`) contains primary branch `br-fragrant-voice-ayq82j6b`, PostgreSQL 18.6. Read-only inventory found no users or accounts in its existing `neondb.neon_auth` schema. That database/schema was preserved.

Created separate database `forge_control_v1` (database ID 2087493) in the same Free project. No project upgrade, new paid service or change to the unrelated second Neon project was made. The project inventory showed approximately 32 MB before this installation; storage and compute usage remain finite and must be checked again before live workloads.

The explicit installer applied existing application migrations 0001/0002/0003/0005, engine 0001–0005 and reviewed auth grants. It adds immutable hash receipts and separate login roles for auth/API/worker/maintenance. The actual non-superuser Neon migration owner successfully installed the SECURITY DEFINER ownership/grants; this was not inferred from the local superuser harness. PUBLIC CONNECT/TEMP is revoked on the new dedicated database and intended runtime groups receive CONNECT.

All four actual runtime connections passed the repository's privilege checks over certificate/hostname-verified TLS: API, worker and maintenance passed `ControlDatabase.check()` against the hosted environment and migration receipt; auth passed `assertAuthDatabase()` with no project-table access, schema creation, owner membership or privileged role inheritance. The privileged `neondb_owner` credential stays private and is excluded from runtime configuration. New credentials are independent, stored in an ignored mode-0600 file, and were not printed or sent to GitHub.

A schema-only rerun checked the exact migration hashes without resetting credentials. Logs are retained in this report's evidence directory. The first installer invocation rejected the CLI-generated URL's unsupported `channel_binding` query option before connecting or mutating anything. The retry explicitly used the connector's supported `sslmode=verify-full` configuration; certificate and hostname verification stayed enabled. No relaxed TLS fallback was added.

**Admission and worker remain disabled; hosted enrollment has no enabled issuer configuration.** No Forge account, model call, generated application, preview, publication or email was created by this installation. This verifies a live database foundation, not the original hosted A–P flow. Vercel environment setup, private email configuration, BYOK connection and all runtime/publication acceptance remain outstanding. Roll back application code without dropping these additive schemas or resetting credentials.
